%h1 Users#show
%p= @user.email

%ul
  - @user.identities.each do |identity|
    %li 
      = identity.provider
      = identity.uid 
      = image_tag "#{identity.info['image']}"
      = radio_button_tag "default_avatar", "#{identity.info['image']}", "#{identity.info['image']}" == "#{@user.avatar}" ? true : false, id: "#{identity.uid}", class: 'avatar', "data-id" => "#{@user.id}"
  %li
    = image_tag "http://www.gravatar.com/avatar/#{@hash}"
    = radio_button_tag "default_avatar", "http://www.gravatar.com/avatar/#{@hash}", "http://www.gravatar.com/avatar/#{@hash}" == "#{@user.avatar}" ? true : false, id: "gravatar", class: "avatar", "data-id" => "#{@user.id}"

%p 
  Choose a default image to use on MADi, or set your own with
  = link_to "Gravatar.com", "http://www.gravatar.com" 
